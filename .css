* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: "Poppins", sans-serif;
}

body {
  background-color: black;
  color: #fff;
}
.header {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  padding: 20px 10%;
  background: transparent;
  display: flex;
  justify-content: space-between;
  align-items: center;
  z-index: 100;
}
.logo {
  font-size: 30px;
  color: #de8110;
  text-decoration: none;
  font-weight: 600;
  cursor: default;
  opacity: 0;
  animation: slideRight 1s ease forwards;
}
.navbar a {
  display: inline-block;
  font-size: 18px;
  color: #fff;
  text-decoration: none;
  font-weight: 500;
  margin-left: 35px;
  transition: 0.3s;
  opacity: 0;
  animation: slideTop 0.5s ease forwards;
  animation-delay: calc(0.2s * var(--i));
}
.navbar a:hover,
.navbar a.active {
  color: #ed7607;
}

.home {
  position: relative;
  width: 100%;
  height: 100hv;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 120px;
  margin: 0px 15px;
  padding-left: 140px;
}
.home-content {
  max-width: 600px;
}
.home-content h3 {
  font-size: 35px;
  font-weight: 700;
  opacity: 0;
  animation: slideBottom 1s ease forwards;
  animation-delay: 0.7s;
  margin-top: 20px;
  margin-bottom: 20px;
}

.home-content h3:nth-last-of-type(2) {
  margin-bottom: 20px;
  animation: slideTop 1s ease forwards;
  animation-delay: 0.7s;
}
.home-content h3 span {
  color: #de8110;
}
.home-content h1 {
  font-size: 56px;
  font-weight: 700;
  margin: -3px 0;
  opacity: 0;
  animation: slideRight 1s ease forwards;
  animation-delay: 0.4s;
}

.home-content p {
  font-size: 16px;
  font-style: oblique;
  opacity: 0;
  animation: slideLeft 1s ease forwards;
  animation-delay: 0.9s;
}
.home-content p span {
  color: #9f38e4;
}
.home-img img {
  max-width: 400px;
  margin-left: 15px;
  margin-right: 57px;
  border-radius: 30px;
  border-style: double;
  border-color: black;
  box-shadow: 0 0 25px #269ec9;
  opacity: 0;
  animation: zoomIN 1s ease forwards, floatImage 4s ease-in-out infinite;
  animation-delay: 1.6s, 2.6s;
}

.social-media a {
  display: inline-flex;
  justify-content: center;
  align-items: center;
  margin: 15px 15px 15px 0px;
  width: 40px;
  height: 40px;
  background: transparent;
  border: 2px solid #de8110;
  border-radius: 50%;
  font-size: 20px;
  color: #de8110;
  text-decoration: none;
  transition: 0.5s ease-in-out;
  opacity: 0;
  animation: slideRight 1s ease forwards;
  animation-delay: 1.5s;
  animation-delay: calc(0.2s * var(--i));
}
.social-media a:hover {
  background: #de8110;
  color: #1f242d;
  box-shadow: 0 0 15px #de8110;
}
.download-btn {
  display: inline-block;
  padding: 10px 18px;
 background: #de8110;
  border-radius: 50px;
  box-shadow: 0 0 15px #de8110;
  font-size: 16px;
  color: black;
  letter-spacing: 1px;
  text-decoration: none;
  font-weight: 600;
  opacity: 0;
  animation: slideTop 1s ease forwards;
  animation-delay: 0.9s;
}

/* keyframe animation */
@keyframes slideRight {
  0% {
    transform: translateX(-100px);
    opacity: 0;
  }
  100% {
    transform: translateX(0);
    opacity: 1;
  }
}
@keyframes slideLeft {
  0% {
    transform: translateX(100px);
    opacity: 0;
  }
  100% {
    transform: translateX(0);
    opacity: 1;
  }
}

@keyframes slideTop {
  0% {
    transform: translateY(100px);
    opacity: 0;
  }
  100% {
    transform: translateY(0);
    opacity: 1;
  }
}

@keyframes slideBottom {
  0% {
    transform: translateY(-100px);
    opacity: 0;
  }
  100% {
    transform: translateX(0);
    opacity: 1;
  }
}
@keyframes zoomIN {
  0% {
    transform: scale(0);
    opacity: 0;
  }
  100% {
    transform: scale(1);
    opacity: 1;
  }
}
@keyframes floatImage {
  0% {
    transform: translateY(0);
  }
  50% {
    transform: translateY(-24px);
  }
  100% {
    transform: translateY(0);
  }
}


.aboutme{
  justify-content: center;
  align-items: center;
  text-align: center;
  opacity: 1;
}
.hello{
  margin-bottom: 10px;
 
}

.about{
  margin: 10px 500px 10px 500px;
  border: 4px solid rgb(247, 169, 23);
   border-radius: 5%;
   box-sizing: border-box;
   line-height: 30px;
}

.skills{
  margin-top: 100px;
  justify-content: center;
  align-items: center;
  text-align: center;
  

}

.box-wrapper{
  display: flex;
  justify-content: space-between;
width: 90%;
  
  
}
#box1{ 
  justify-content: center;
  align-items: center;
  text-align: center;
  margin-left: 100px;
  
  margin-right: 10px; 
  line-height: 30px;
  
  
}
#box2{ 
  justify-content: center;
  align-items: center;
  text-align: center;
  margin-left: 100px;
  
  margin-right: 10px; 
  line-height: 30px;
  
}
#box3{ 
  justify-content: center;
  align-items: center;
  text-align: center;
  margin-left: 100px;
 
  margin-right: 10px; 
  line-height: 30px;
  
}
#box4{ 
  justify-content: center;
  align-items: center;
  text-align: center;
  margin-left: 100px;
  
  margin-right: 10px; 
  line-height: 30px;
  
}
 
.image{
  
  width: 40px;
}

